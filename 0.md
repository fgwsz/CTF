# AWD 红蓝对抗通用应急加固完整策略（新版无自杀、双系统兼容、满分防御流程）
## 核心总口诀（操作顺序，得分优先级从高到低）
备份所有配置 → 区分环境识别服务 → PHP安全加固 → Web服务(Nginx/Apache)加固 → 系统底层权限加固 → SSH远程防爆破 → 防火墙端口封堵 → MySQL数据库加固 → 源码漏洞批量检索修复 → 恶意后门排查清理 → 持久化防护部署 → 多重自检校验 → 提交Flag
通用规范：
1. 所有配置修改前必备份`.bak`，配置出错一键回滚，杜绝靶机报废；
2. CentOS使用`yum、systemctl、firewalld`；Ubuntu使用`apt、systemctl、ufw`；
3. 路径默认网站根目录`/var/www/html`，靶机路径不同全局替换；
4. 命令全部英文半角符号，nano编辑器：Ctrl+O保存回车、Ctrl+X退出；
5. 只读排查命令不会破坏业务，修改类命令全部附带风险注释。

# 第一部分 前置基础：环境识别 + 全配置备份（必做，得分基础）
## 1. 快速区分Web运行环境
```bash
# 判断PHP运行模式，选择对应重启命令
ps aux | grep php
# 出现php-fpm → PHP-FPM+Nginx
# 出现httpd/apache2 → Apache
# 查看Web服务
ps aux | grep nginx
ps aux | grep httpd
ps aux | grep apache2
```

## 2. 统一备份模板（所有配置文件通用）
```bash
# 修改配置前强制备份
cp 原配置文件 原配置文件.bak
# 语法检测+自动回滚逻辑（示例Nginx）
nginx -t && systemctl reload nginx || cp /etc/nginx/nginx.conf.bak /etc/nginx/nginx.conf
```

## 3. 网站源码完整备份（防止源码被篡改删除）
```bash
cp -r /var/www/html /tmp/www_bak_$(date +%H%M)
```

# 第二部分 核心Web服务加固（分值最高，优先完成）
## 一、PHP 加固（防御文件包含、命令执行、信息泄露）
### 1. 定位并编辑php.ini
```bash
php --ini # 查看配置真实路径
nano /etc/php.ini
```
### 2. 安全配置（直接覆盖，兼容ThinkPHP/Laravel）
```ini
allow_url_fopen = Off
allow_url_include = Off
display_errors = Off
expose_php = Off
# 禁用全部高危执行、代码操作函数
disable_functions = system,exec,shell_exec,passthru,eval,assert,popen,proc_open,dl,pcntl_exec,putenv,ini_set,file_put_contents,create_function
# 限制目录访问，保留/tmp缓存目录避免框架白屏
open_basedir = /var/www/html:/tmp
```
### 3. 重启生效（二选一）
```bash
systemctl restart php-fpm
# Ubuntu Apache环境：systemctl restart apache2
```
### 4. PHP自检验证
```bash
# 验证高危函数已禁用
php -r "echo function_exists('system')?'风险':'已禁用';"
# 测试命令执行是否拦截
php -r "@system('id');"
```

## 二、Nginx 完整加固（防版本泄露、目录遍历、上传木马、注入攻击）
1. 备份配置
```bash
cp /etc/nginx/nginx.conf /etc/nginx/nginx.conf.bak
nano /etc/nginx/nginx.conf
```
2. 在`http{}`全局段添加安全参数
```nginx
server_tokens off;
autoindex off;
```
3. 编辑站点虚拟主机 `nano /etc/nginx/conf.d/default.conf`
```nginx
server {
    listen 80;
    server_name localhost;
    root /var/www/html;
    index index.html index.htm index.php;

    location / {
        # 简易WAF拦截注入、目录穿越、恶意载荷
        if ($query_string ~* "(eval|system|proc_open|\.\./|union|select|insert|delete)") {
            return 403;
        }
        try_files $uri $uri/ /index.php?$query_string;
    }

    # 拦截上传目录所有php、phtml、phar、php5等可执行脚本
    location ~* ^/upload/.*\.(php|phtml|phar|php5|shtml)$ {
        deny all;
    }

    # PHP脚本仅允许GET/POST，拦截PUT/DELETE危险请求
    location ~ \.php$ {
        if ($request_method !~ ^(GET|POST)$ ) {
            return 403;
        }
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```
4. 语法校验重载，出错自动回滚
```bash
nginx -t && systemctl reload nginx || mv /etc/nginx/nginx.conf.bak /etc/nginx/nginx.conf
```

## 三、Apache / httpd 加固
```bash
cp /etc/httpd/conf/httpd.conf /etc/httpd/conf/httpd.conf.bak
nano /etc/httpd/conf/httpd.conf
```
修改参数：
```apache
ServerTokens Prod
ServerSignature Off
Options -Indexes
```
### 上传目录禁止解析PHP（新增配置）
```apache
<Directory "/var/www/html/upload">
    php_admin_flag engine off
</Directory>
```
重启服务
```bash
systemctl restart httpd
# Ubuntu：systemctl restart apache2
```

# 第三部分 系统底层安全加固
## 1. 文件权限最小化配置
```bash
# 网站目录基础权限
chmod -R 755 /var/www/html
# 上传目录属主root，仅开放写入
chown root:www-data /var/www/html/upload
chmod 755 /var/www/html/upload
# 锁定首页防止篡改（需修改页面执行chattr -i解锁）
chattr +i /var/www/html/index.php
# 日志文件收紧权限（仅root可读）
find /var/log -type f -exec chmod 600 {} \;
```

## 2. SSH远程登录防爆破（无锁机风险，新版优化）
```bash
find / -name sshd_config 2>/dev/null
cp /etc/ssh/sshd_config /etc/ssh/sshd_config.bak
nano /etc/ssh/sshd_config
```
修改内容：
```ini
PermitRootLogin no
MaxAuthTries 5
# PasswordAuthentication no 注释！无密钥环境取消注释会直接断连
```
重启服务
```bash
systemctl restart sshd
```

## 3. 防火墙加固（CentOS firewalld + Ubuntu ufw双版本）
### CentOS firewalld
```bash
# 永久关闭高危端口3306、6379、445、135
firewall-cmd --permanent --remove-port=3306/tcp
firewall-cmd --permanent --remove-port=6379/tcp
firewall-cmd --permanent --remove-port=445/tcp
firewall-cmd --reload
# 临时兜底阻断（内存生效，重启失效）
iptables -I INPUT -p tcp --dport 3306 -j DROP
iptables -I INPUT -p tcp --dport 6379 -j DROP
iptables -I INPUT -p tcp --dport 445 -j DROP
```
### Ubuntu ufw
```bash
ufw enable
ufw deny 3306/tcp
ufw deny 6379/tcp
ufw deny 445/tcp
ufw reload
```

## 4. 账号与密码安全策略
```bash
nano /etc/login.defs
```
```ini
PASS_MIN_LEN 8
PASS_MAX_DAYS 90
PASS_MIN_DAYS 7
```
```bash
# 锁定闲置测试账号
passwd -l test
# 影子密码文件权限保护
chmod 600 /etc/shadow
```

## 5. 日志审计开启
```bash
systemctl enable --now rsyslog
find /var/log -type f -exec chmod 600 {} \;
```

# 第四部分 MySQL 数据库加固（防止webshell提权拖库）
```bash
# 优先绑定本地访问，禁止远程连接
echo "bind-address = 127.0.0.1" >> /etc/my.cnf
# 本地清空root远程访问权限
mysql -uroot -e "UPDATE mysql.user SET authentication_string='' WHERE user='root';FLUSH PRIVILEGES;"
# MySQL5.6兼容备用
# mysql -uroot -e "UPDATE mysql.user SET password='' WHERE user='root';FLUSH PRIVILEGES;"
systemctl restart mysqld
# Ubuntu：systemctl restart mysql
```

# 第五部分 源码漏洞批量检索与修复（靶场提供源码必做，高分项）
## 1. 批量漏洞检索命令（仅查询，不修改）
```bash
# 1. 文件包含漏洞
grep -rE "(include|require)(_once)?\s*\(\s*\$_(GET|POST|REQUEST)" /var/www/html --include="*.php"
# 2. SQL注入（无预编译拼接变量）
grep -rE "SELECT|INSERT|UPDATE|DELETE.*\$_(GET|POST)" /var/www/html | grep -v prepare
# 3. XSS无转义输出
grep -rE "echo|print\s*\$_(GET|POST)" /var/www/html | grep -v htmlspecialchars
# 4. 危险代码/命令执行函数
grep -rE "eval|assert|system|popen" /var/www/html --include="*.php"
# 5. 反序列化风险
grep -r "unserialize\s*\(\s*\$_(GET|POST)" /var/www/html
```
## 2. 标准漏洞修复模板
1. 文件包含（白名单）
```php
$allow = ["index.php","home.php"];
$page = $_GET['page'];
if(in_array($page,$allow)) include($page);
```
2. SQL注入（参数化预编译）
```php
$stmt = $conn->prepare("select * from user where name=?");
$stmt->bind_param("s",$_GET['name']);
$stmt->execute();
```
3. XSS修复
```php
echo htmlspecialchars($_GET['data'],ENT_QUOTES);
```
4. 反序列化替换
```php
# 禁止 unserialize($_GET['pop']);
$data = json_decode($_GET['pop'],true);
```
## 3. 业务逻辑漏洞修复
- 越权访问：查询SQL强制绑定当前登录用户session_id；
- 验证码绕过：后端session绑定验证码，拒绝前端仅校验；
- 支付篡改：金额、订单状态以数据库为准，不接收前端可控金额。

# 第六部分 后门查杀、应急清理（新增补充优化点，无扣分）
## 1. Webshell 木马检索
```bash
# 检索一句话木马特征，人工确认再删除
grep -rE "eval\(|@eval|assert|file_put_contents|base64_decode" /var/www/html --include="*.php"
# 清理上传目录恶意脚本（谨慎执行，会删除所有php）
find /var/www/html/upload -name "*.php" -delete
```
## 2. 持久化后门排查（crontab）
```bash
# 只查看，禁止直接清空全部定时任务
crontab -l
cat /etc/crontab
chmod 600 /etc/crontab
```
## 3. 异常进程、外联恶意IP排查
```bash
# 查看对外恶意连接
netstat -antp | grep -v 127.0.0.1
# 查看5分钟内新增修改的可疑PHP木马
find /var/www/html -name "*.php" -mmin -5
# 关闭高危Redis服务
systemctl stop redis && systemctl disable redis
# /tmp目录禁止二进制执行
mount -o remount,noexec /tmp
# 重启php-fpm清空内存后门
systemctl restart php-fpm
```

# 第七部分 长效持久化防护
## 1. 文件指纹监控（提交Flag前必执行）
```bash
# 生成源码校验指纹
find /var/www/html -name "*.php" -exec md5sum {} \; > /tmp/php_md5.txt
# 对比是否存在篡改文件
find /var/www/html -name "*.php" -exec md5sum {} \; | diff /tmp/php_md5.txt -
```
## 2. 定时自动清理/tmp下恶意php文件
```bash
# 每1分钟删除/tmp中5分钟以上PHP文件，不影响业务缓存
echo "*/1 * * * * find /tmp -name '*.php' -mmin +5 -delete" >> /etc/crontab
```

# 第八部分 Windows靶场简易加固（配套补充）
Win+R快速工具：
1. secpol.msc 本地安全策略：密码长度8位、5次锁定、开启登录审计
2. inetmgr IIS管理器：关闭WebDAV、upload目录移除脚本执行、关闭目录浏览
3. wf.msc 高级防火墙：禁用3389、445、1433高危端口
4. lusrmgr.msc 用户管理：禁用Guest，删除陌生管理员
5. 管理员CMD：gpupdate /force 刷新组策略

# 第九部分 赛场故障急救回滚大全
```bash
# Nginx配置崩溃恢复
mv /etc/nginx/nginx.conf.bak /etc/nginx/nginx.conf && systemctl reload nginx
# PHP配置损坏恢复
cp /etc/php.ini.bak /etc/php.ini && systemctl restart php-fpm
# SSH配置锁机恢复（控制台专用）
cp /etc/ssh/sshd_config.bak /etc/ssh/sshd_config && systemctl restart sshd
# 首页锁定无法更新
chattr -i /var/www/html/index.php
# Nginx WAF误杀base64图片上传
nano /etc/nginx/conf.d/default.conf # 删除location / 中的base64关键词
nginx -t && systemctl reload nginx
```

# 第十部分 交Flag前三重自检流程（30秒完成）
1. PHP高危函数校验
2. PHP文件md5指纹比对，确认无篡改后门
3. Nginx/Apache配置语法检测，无报错

# 新版策略优化亮点对比旧版
1. 整合全套补充优化点：webshell查杀、Ubuntu防火墙适配、异常外联排查、定时任务后门检测、phtml/phar后缀绕过拦截，全部纯加分无失分；
2. 严格区分只读查询命令与修改类高危命令，每一条风险操作添加注释，避免赛场误操作丢分；
3. 双系统全覆盖（CentOS+Ubuntu+Windows），适配全部比赛靶机；
4. 所有自杀操作彻底规避：SSH密码登录不关闭、open_basedir保留/tmp、定时清理缓存延时5分钟，不会破坏业务；
5. 分层模块化，逻辑清晰，打印后正反面分区，查找效率更高；
6. 新增大量实战后门排查流程，针对AWD对手常用持久化攻击手段专项防御；
7. 全套回滚方案覆盖所有服务，配置出错无需重置靶机，守住分数。

# 赛场保命三条准则
1. 活着优先：绝不注释打开PasswordAuthentication no，仅禁止root登录防爆破；
2. 业务优先：open_basedir必须携带/tmp，保证框架session正常读写；
3. 操作兜底：任何配置修改先备份，出现异常优先回滚，不要反复调试浪费时间。
